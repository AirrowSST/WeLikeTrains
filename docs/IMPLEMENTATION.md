# Implementation tracker

Primary journey: Rachel, Tampines to Raffles Place, 07:40 departure / 08:45 deadline. Additional profiles: Arjun (comfort/cycle), Mdm Lim (step-free / low walking speed).

## Scope

- Audit follow-up (2026-09-19): removed RoadWorks polling, diagnostics and planner road-work warnings by user request; corrected search copy to indexed stations, bus stops and landmarks. P0 marked verified by user attestation; remaining P1/P2 tracked in `docs/AUDIT-TRIAGE.md`. TypeScript and 17 feed tests pass. No deployment performed.

- Mobile-only PWA: one column, bottom navigation and 480px maximum app width at every viewport; official OneMap tiles for detailed online display, a committed OSM fallback, and local named-stop search, graph routing and door-to-door legs.
- Normal live entry automatically requests a one-shot foreground browser location, starts with a blank **Where to?** destination and defaults departure to **Now**. Permission failures remain visible and the origin stays editable. Active continuous tracking still stops with the journey dialog; manual step confirmation remains authoritative. Demo mode uses deterministic, visibly labelled simulated positions and never requests geolocation permission.
- Original vs alternative route map, affected segments, text-labelled crowds, uncertainty.
- Live LTA adapters with canonical lines, independent health/freshness and cached weather; labelled deterministic replay.
- Planned events, lift outages, rain and crowd forecasts affect route ranking.
- Grounded Vertex AI assistant with bounded function calls for preference proposals, supplied-route recommendations and validated in-chat route cards, recent conversational context, browser voice-to-text, speech playback, transparent disruption-risk estimation and reproducible evaluation.
- Local routine/journey persistence, opt-in background web push with Firestore + Cloud Scheduler.
- Guest-first preferences and multiple daily commutes, with optional server-verified Google sign-in that merges guest state into a Firestore-backed account. Rachel, Arjun and Mdm Lim are isolated faux accounts under Account → Developer mode, with deterministic location, scenario and custom weather state that is discarded on exit.
- A phone-sized, first-run guest onboarding screen for the existing route and readability preferences, with an equally available skip path and later editing under Preferences.
- Cloud Run, Secret Manager, least-privilege service accounts, first-time infrastructure setup and guarded manual code deployment from the authenticated development device.
- Requirements matrix, assumptions, data licences, demo and clean-machine instructions.

## Constraints discovered

The remote was initially empty. The organiser's referenced PS2/data and PS2/references remain absent. Submission instructions were subsequently supplied at `C:\Users\Yaw Tia\Downloads\README.md` for future context only; the user explicitly deferred write-up/submission pending human polishing. Google Cloud CLI, authenticated access, Secret Manager integrations and Cloud Run deployment are now configured. OSM data is fetched during explicit maintenance and bundled; OneMap is the only runtime public map-tile dependency and the app does not query Overpass at runtime.

## Engineering verification — 2026-09-19

- Fixed the Journey sheet recommendation copy overlapping the first route card on short phone viewports. The sheet's promoted content sections now retain their natural height and overflow through the existing inner scroller instead of flex-shrinking; this covers long recommendations such as multi-mode SLRT/bus guidance. TypeScript and the production client/server build passed, and 6 focused phone/wide recommendation and draggable-sheet checks passed. No commit, push or deployment performed.

- The **One step at a time** navigation sheet now reuses the home journey panel's drag hook and grip. Touch/pointer dragging, grip tapping and Arrow/Home/End keys resize it to expanded, middle and collapsed snaps; attribution follows the sheet while the map remains full-height. The reusable hook accepts sheet selectors and sizing options without changing the home panel's established snap behavior. TypeScript and the production client/server build passed; focused active-navigation and shared home-sheet drag checks pass on phone and wide-screen projects. No commit, push or deployment performed.

- Starting a route now opens a full-phone, map-first navigation state instead of a centred directions popup. The normal app chrome is covered, the current leg is fitted and emphasised, comparison clutter is suppressed, and the existing directions, arrivals, foreground-location state, completion rewards and manual progress controls remain available in a compact scrollable bottom sheet. The phone frame remains capped at 480px on wide displays. Whole-project TypeScript and the production client/server build passed; 4 focused route-selection/navigation checks and all 10 rewards/arrival journey checks passed across phone and wide-screen projects, including 320px overflow, map/sheet geometry and current-leg visibility. No commit, push or deployment performed for these local changes.

- Route plans no longer imply that the recommendation has been selected. No Start action or checked selection control appears until the commuter explicitly taps a route; the only Start action then appears inside that highlighted card and includes the route title. Selecting another route moves the action with the selection, while newly computed plans clear it. Blocked routes remain non-startable.

- Integrated remote `main` at `dd72864` with the local rewards, arrival-board and loopback-origin changes. Resolved additive conflicts by retaining both the upstream crowd styles/routing notes and local rewards/arrivals styles/notes. Updated local dependencies without changing the lockfile. Post-integration TypeScript, production client/server build, 29 focused unit tests and all 10 targeted rewards/arrivals phone/wide browser checks passed. Local edits remain uncommitted; a pre-pull backup stash is retained. No push or deployment performed.

- DataMall-first bus pivot (supersedes the earlier 890-hop enrichment below): DataMall now owns bus stop/service/direction connectivity. The accepted graph has 25,682 hops across 601 services / 797 directions, with 842 optional same-service OSM shapes and 24,840 labelled schematic hops. Rejected 343 gapped or inconsistent-distance adjacencies rather than inventing a connection or an implausibly short ride. This does not certify complete services or islandwide walking access. Repeated stop occurrences carry sequence identity; no straight-line geometry is used for road-specific congestion/flood claims. Walking/rail remain on the local OSM graph.
- The larger graph uses spatially indexed nearby-stop connections, an optimistic reverse-graph lower bound for A*, and reused transfer walks. Starting and ending at the exact same official stop no longer invents a walk out to an OSM node and back. Schematic bus lines are labelled in normal directions, active journeys and map popups. No deployment was performed.
- Pivot verification: final production build/typecheck passed. Full unit run: 92/93 passed; the sole failure was an outdated assertion expecting no NTU-prefix results before the DataMall NTUC stops were indexed. Updated that assertion to preserve word-prefix matching and reject Century substring matches, with explicit DataMall place IDs. The final targeted search/bus-graph rerun passed all 11 tests. The full run passed the planner, timeline and missed-first-bus simulator regressions. Two targeted mobile browser checks passed (train popup and labelled schematic bus popup at 320px). Final local synthetic evaluation recorded zero structural failures. Cold first-plan initialization remains noticeable (about 22 seconds in the concurrent local evaluation run); warm scenario timings varied, so this is not a latency benchmark or field-accuracy claim.

- Routing-source integration and crowdedness: official bus reference joins now enrich 890 directed OSM hops; imported 613 station exits, 7,012 covered-linkway features and 4,830 cycling-path features. Added guarded GTFS realtime decoding, released-only planned bus overlays, conservative traffic slowdowns, corrected NEA station observations and a consented local timing-evaluation command. See `ROUTING-DATA-STATUS.md` for source-by-source limits. Both official train realtime metadata endpoints returned no downloadable payload during the check; fixture success is not live-provider verification. The planned-bus import found zero eligible released changes. No field calibration or deployment was performed.
- Bus/train crowdedness is visible in directions, active-journey steps and route map popups: Low, Moderate, High or Unknown. Bus labels describe the selected arriving vehicle; rail labels describe station/platform density, not carriage occupancy. Current/forecast/simulated provenance is explicit; expired cached or invalid/undated platform observations are not presented as current. Saved offline plans are not refreshed live readings.
- Verification for this slice: production build passed; full unit run passed 86 tests; following the final malformed/undated-reading guard, typecheck and 27 targeted crowd/feed/routing-source tests passed. Two targeted mobile browser checks passed for train and bus path details, including bus popup containment at 320px (the assertion waits for Leaflet's automatic pan to settle). Local synthetic evaluation recorded zero structural failures. These are automated/local checks, not real-phone, live-disruption or route-accuracy sign-off.

- Active journeys now show up to three upcoming services in **One step at a time**, matching the current or next transit leg's boarding stop, bus service or rail direction. Fresh monitored bus ETAs use the existing LTA adapter; rail departures use a validated, local-only `/api/train-arrivals` lookup against the committed GTFS snapshot and are explicitly scheduled, not live tracking. Polling runs every 30 seconds while the popup is visible, with manual refresh, request cancellation on step change/close and visible offline/stale/unavailable states. Stale bus countdowns are removed instead of presented as current arrivals. Developer journeys use snapshotted synthetic bus data and a labelled step-based demo clock without calling live bus feeds.
- Arrival verification: whole-project TypeScript, production client/server build and all 89 unit tests passed. Four targeted mobile/wide browser checks passed, covering stop/service selection, upcoming boarding while walking, polling, refresh, stale/offline behaviour, direction-specific train requests, demo isolation, polling cleanup, 320px overflow and scoped axe. A local EWL timetable API lookup returned three scheduled departures for the requested direction. These are local automated checks, not live-provider accuracy or physical-phone validation. No deployment performed.

- Rewards now replaces Preferences in the bottom navigation; Account opens the Preferences subpage, and the top-left points counter opens Rewards. Cards preview points before selection; final manual journey completion credits the snapshotted route once per departure/endpoint pair. Walking/cycling earns 1 point per 100 m and a lower known crowd level than the original route adds 20; blocked routes earn zero. A refresh of route conditions cannot change the active journey's promised award. Fictional coffee/smoothie/cycle rewards cost 50/100/200 points with explicit confirmation, balance deduction and a My rewards record. All catalogue items are labelled demo-only with no real-world value.
- Points are an on-device prototype ledger, separated by guest and signed-in email; account preference/commute sync does not upload or merge it. Developer-demo wallets live only in memory and reset on exit or preset changes. Completion is self-reported, not anti-fraud or measured-emissions verification. Preference saving now persists settings without implicitly saving a new commute. Automated checks: whole-project TypeScript, production client/server build and 86 unit tests passed. Six targeted mobile/wide rewards checks passed for point previews, abandonment, completion, duplicate prevention, redemption, persistence, account/demo isolation, 320px overflow and scoped axe. Existing phone planning/compare/save, large-text/offline and companion-consent revocation checks also passed (the consent test's obsolete chatbot locator was updated to the current accessible label). No deployment performed.

- Local development no longer fails with **Request origin is not allowed** when Vite advances from occupied port 5173 to another loopback port. The API origin policy now permits an HTTP loopback UI on any port only when the API request host is also loopback; public hosts still require the configured public origin or same origin. Four focused policy cases, all 80 unit tests, whole-project TypeScript, a live `localhost:5174` → `localhost:8080` request and a rejected external-origin request passed. The visible localhost app was reloaded and the origin error was absent. No deployment was performed.

- Timeline verification: production build/typecheck passed; the full unit run passed 75 tests. After tightening fixed bus departure slots, all 13 focused timeline/simulator tests passed, including all six timeline IDs, exact event boundaries, recovery, HTTP payload parity and account rejection. Four targeted mobile browser checks passed for playback/pause/restart, rain blocking, route comparison and guest restoration; playback also checked 320px overflow. Local synthetic evaluation recorded zero structural failures. No deployment was performed.

- Immediate-start timeline verification: all 6 focused timeline unit tests pass with disruption/lift and rain active at minute 0, and the targeted playback/restart/recovery browser flow passes in both phone and wide-screen phone-shell projects. Fresh client and server bundles build successfully. The aggregate `npm run build` typecheck is presently blocked by a separate in-progress mock typing change in `tests/feeds.test.ts`; that file was not changed for this timeline update. No deployment was performed.

- Developer demos now offer two deterministic timelines per profile: control (clear, no events) and eventful (profile-specific transport event, heavy rain and recovery). Every adverse condition in an eventful timeline is active from minute 0, so the initial plan already reflects both heavy rain and the persona-specific rail disruption or lift outage; weather clears at +25 and transport recovers at +40. The UI replaces the custom weather/network selectors with playback, pause, restart, speed and event jumps. Fixed simulation dates and per-request selection drive raw authored DataMall/NEA inputs shared with the loopback HTTP simulator; in-app demos need no running simulator process or external provider. Existing standalone scenarios remain internal regression fixtures. Timeline requests are validated as demo-only, clock-consistent and unsyncable. Journey progress remains manual; playback replans from the selected origin and does not model automatic movement.

- Each selected journey leg on the map is now a generous touch and keyboard target. Choosing a path opens a compact map popup with the relevant mode or service, boarding and alighting points, direction, duration, wait, crowding and timing-source details. Original-route comparison overlays remain non-interactive so path selection is unambiguous.
- Journey-path popup verification: the production build/typecheck passes; targeted Playwright checks pass for a train path on the normal phone and wide phone-shell projects, plus a Bus 2 alternative selected by keyboard at 320 px. No full browser-suite run or deployment was performed for this slice.
- An opt-in local DataMall HTTP simulator now supports bus arrivals, 500-row bus-reference pagination, authored GTFS ZIP links with 15-minute expiry, and deterministic failure scenarios. `npm run dev:datamall` binds only to loopback; development `LTA_BASE_URL` overrides use a fixed dummy key, isolate provider caches and label simulated feed rows/notices. Production rejects overrides. The GTFS importer restricts simulated output to labelled ignored `.cache/` artifacts. Weather and map providers remain independent. Tests exercise the real HTTP boundary, cache degradation, credential isolation, partial failure, expiry, malformed JSON, timeouts and production guards.
- Simulator verification: whole-project TypeScript and all 75 unit tests pass. A local CLI/import smoke check downloaded the authored fixture ZIP and produced six labelled synthetic trips under `.cache/datamall/`; official schedule files were untouched. The fixed-clock HTTP/planner regression exercises a real OSM Bus 27 journey: normal service boards at 07:50 after a two-minute stop wait, while `missed-first-bus` ignores the departed 07:46 vehicle, boards at 07:51, changes occupancy from low to moderate and moves the route arrival one minute later. A local `/api/plan` smoke also confirmed that an independent weather delay shifts the stop-ready time and causes the arrival to be reselected consistently. No cloud request or deployment was performed for this slice.

- The persistent top identity bar has been removed. The phone bottom navigation now includes dedicated **Preferences** and **Account** destinations alongside Journey, Disruptions and Commutes; both render as full peer pages rather than popup dialogs, and preferences no longer live inside the account pane. **Journey** is the centered, raised accent action. On the journey map, notifications are a single circular bell at the upper right, stacked above the matching circular recenter control; Leaflet zoom buttons are intentionally removed so route-status messages remain clear. Help is available from Account.
- Fresh local guests now see a one-time mobile onboarding screen before normal journey planning. They can save step-free, shelter, crowd, cycling, walking-limit, delay-alert and larger-text preferences through the existing guest-state path, or skip and keep the defaults. Completion is stored separately from guest data, authenticated session restoration dismisses the screen, developer demos never open it, and deleting all saved data makes onboarding available again.
- Online map display uses official OneMap Default tiles across the documented whole-Singapore bounds. The committed OSM vectors remain a visible offline/failure fallback. Place search and route computation use only committed OSM-derived files; OneMap is not used for geocoding, authentication or itineraries. Search coverage is deliberately limited to curated places and named stops in the extract.
- Navigate search now keeps all ten curated destinations in the default list, returns up to twelve ranked matches, prioritises exact rail stations over loosely matching bus stops, and matches word prefixes instead of arbitrary substrings. This prevents supported locations such as Tampines MRT from being hidden and avoids false results such as `NTU` matching the middle of “Century.” Locations outside the curated catalog and committed named-stop index remain unavailable until explicitly added to the local dataset.
- Fresh guest journeys no longer open as Rachel's Tampines Central → Raffles Place preset. Live mode requests a one-shot device location automatically, presents a **Where to?** destination prompt, and keeps route planning disabled until both endpoints are known. **Leave** defaults to **Now** and refreshes the timestamp when planning; a manual time remains available. Saved commutes and isolated developer presets retain their own routes and times. When the map centers on a location, it biases the marker upward according to the current journey-sheet overlap so the indicator remains clear of the bottom pane.
- Leave and arrival time controls now open a Wayce-styled, accessible time dialog instead of the browser's visually inconsistent native picker. The dialog provides large hour/minute steppers, an AM/PM toggle, explicit Cancel/Set actions and touch targets sized for the phone interface.
- Pedestrian routing now indexes every substantial connected component in the bundled OSM extract, while matching both ends of a walking leg to the same component. This restores searched western journeys such as NTU to Raffles Place without treating small isolated indoor paths as routable coverage.
- The product name shown in the interface, install metadata, notifications and companion identity is now Wayce. Satoshi is the single interface typeface, delivered from Fontshare’s official CDN under the ITF Free Font License; system sans-serif remains the offline fallback because the licence does not permit self-hosting the font files.
- Reworked the journey pane as a viewport-bound bottom sheet over a fixed full-height map, matching the Grab-style interaction requested during polishing. The Journey document no longer scrolls: mouse, touch and keyboard resizing change only the sheet height, while route and planner content scroll inside the sheet with contained overscroll. Full expansion reaches the top and keeps a centered, bouncing **Show map** control with a map icon and double chevrons below the drag bar available; collapse restores the map without resizing it. Targeted phone and wide-viewport regression checks cover fixed document scroll, physical grip movement, all snap states, internal content scrolling and the 480px shell. A direct port-5173 check confirmed the document stayed at `scrollY = 0` with viewport-height page geometry. Physical-phone verification remains pending.
- The Navigate form removes decorative origin, destination, time, location and action icons, uses title-case “Leave” and “Arrive” labels with quieter typography, and omits the redundant origin/destination swap action. Preferences remains available as a labelled text control.
- The mobile journey map now distinguishes walking, bus, rail and cycling geometry with mode icons, labels and a compact legend; selected endpoints and a small set of Singapore landmarks provide orientation. Weather-affected walking areas use clearly labelled, approximate rain highlights rather than claiming radar-level precision.
- The map now adds small clickable MRT/LRT station markers at ordinary journey zoom and bus-stop markers when zoomed in. Only stops in the visible area are mounted, keeping the phone map responsive; opening a marker shows the mapped name, stop/station code and bundled service or line references. Rail markers come from the committed OSM transit extract; bus markers now use the complete bundled DataMall reference snapshot. Both remain keyboard reachable and introduce no runtime public transit lookup dependency.
- The explicit `npm run data:buses` maintenance import walks every 500-row page from official DataMall BusStops, BusRoutes and BusServices, validates and deterministically compresses the results, and records licence, counts and SHA-256 provenance without storing the AccountKey. The 2026-09-19 snapshot contains 5,208 stops, 26,823 ordered route-stop rows and 801 service directions with zero missing stop joins. Cloud Run will receive it through the existing container data copy on the next explicitly authorised deployment; no deployment was performed for this slice. Planner geometry remains honestly limited to selected OSM bus relations pending verified geometry joins.
- When conditions change the recommended route, the map now keeps the full original route as a muted dashed line, marks only the affected original subsection with a distinct dotted treatment and text label, and draws the revised route as the dominant solid path. A targeted browser regression checks all three layers on phone and wide viewports.
- Every primary route card now shows a text-labelled low, moderate, high or unknown crowd state. The main recommendation banner displays the planner's actionable reason and timing advice directly instead of only reporting route count and arrival time.
- Plans now carry an explicit `travelDecision`. If every mapped option is blocked by unsafe heavy weather, Wayce returns `wait`, tells the commuter to wait and re-plan, labels the first card accordingly, and does not append misleading arrival or earlier-departure advice to the blocked route preview.
- Foreground proactive warnings and background push reminders now use the same saved delay-alert threshold. The boundary is inclusive, so a 15-minute delay triggers a 15-minute threshold, and the reminder title and body no longer depend on a separate hard-coded cutoff.
- The detailed OneMap layer is visibly labelled as online data and carries the required SLA attribution. The compact committed OSM vectors remain underneath it and are visibly labelled when used as the offline fallback.
- The first routing-timing data audit now uses the current DataMall paths for `v4/TrafficSpeedBands`, `EstTravelTimes` and `PubFloodAlerts`. Dedicated parsers retain road-segment geometry and speed ranges, expressway section ETAs, and CAP-style flood timing/location fields; deterministic fixtures cover partial outages and stale-cache fallback. Road speed and section ETA remain informational with zero additive delay until later route matching is implemented, so this adapter correction does not silently change journey ranking.
- Rail crowd forecasts and bus occupancy now use the time the commuter is expected to board each segment instead of the overall trip departure. For live bus candidates, the planner replaces the fixed boarding allowance with the first fresh monitored DataMall arrival that remains catchable after the access walk, then rechecks downstream buses after earlier condition delays, advances transfers chronologically and reranks routes. Stale, unmonitored, absent or out-of-horizon arrivals retain an explicitly labelled local wait estimate; bus in-vehicle time remains estimated.
- Rail timing now uses a committed, gzip-compressed derived copy of LTA DataMall GTFS Schedule (Train), refreshed only through the explicit authenticated `npm run data:gtfs` maintenance command. Provenance records the 2026-09-19 access, Singapore Open Data Licence v1.0, SHA-256 hash, 17,576 trips, 12 service calendars and 2026 service-date coverage without retaining the AccountKey or expiring archive URL. The evaluator applies calendar exceptions, supports times beyond 24:00, normalizes platform IDs, advances through estimated station access and transfers, selects the first train that remains catchable and visibly falls back to the local distance estimate for missing, corrupt, unmapped or out-of-coverage data. Rachel's 2026-09-21 plan selects the 07:57:20 EWL service with a 36:20 scheduled ride and an 08:36 door-to-door arrival; this is planned service, not an actual-movement or accuracy claim.
- Production build and whole-project TypeScript pass with the committed schedule asset and server timing integration.
- 75 unit tests pass, including deployment guardrails, complete DataMall bus-reference pagination and snapshot loading, real OSM Rachel/Arjun/Mdm Lim routes, map transit-stop data, first-catchable live bus timing, chronological bus transfers, condition-delay reselection, missed-bus reranking, stale-arrival fallback, segment-time crowd selection, spatial traffic relevance, saved delay-threshold boundaries, dated GTFS service selection, calendar exceptions, beyond-24:00 trips, chronological rail transfers, corrupt-snapshot degradation and visible timetable fallback, DTL crowd-code join, current DataMall traffic/flood adapter contracts and feed-degradation handling, safe heavy-weather decisions, location handling, session tamper/expiry handling, demo/account validation boundaries and server validation of chat tool calls.
- The Stage 1 server/import files pass a targeted TypeScript compile, and the whole-project `npm run check` passes after the concurrent UI edits stabilized.
- Browser coverage stubs OneMap tiles so tests remain deterministic and no live provider is required. It separately checks the detailed online state and bundled offline fallback alongside local search/routing, account/demo isolation, the resizable one-column layout, chat route cards, foreground/simulated location, 320px no-overflow, axe and offline reload. The full-height sheet, **Show map** control and downward-drag recovery pass four targeted checks across the phone and wide-screen-mobile projects, including a 320px overflow assertion; the drag regression sends a real Chromium touch sequence for an ordinary 96px pull and checks three intermediate mouse positions to ensure the pane follows the pointer before release. The browser voice-input regression passes at 320px with a mocked recognition provider, no composer overflow and no scoped WCAG A/AA violations; real microphone behavior remains physical-device QA. The local transit-marker regression passes separately on the phone and wide-screen-mobile projects, covering rail details at journey zoom, close-zoom bus details, touch activation and keyboard activation. The previous 20 Chromium checks passed when run as separate projects, and a targeted Google sign-in timestamp regression also passed.
- Local evaluation: nine synthetic journeys plus six preference cases pass their structural checks. Rachel/rain and Arjun/rain remain blocked route previews, but now pass because the typed decision is explicitly `wait`, not because the routes were relabelled as usable. Previously recorded cloud evaluation used Vertex AI for all nine journey cases. `docs/evaluation-*.json` contain exact outputs, not a general AI accuracy claim. Model-generated prose still needs human review.
- Hosted Cloud TTS returned MP3 and the real LTA integration was exercised. Scheduler completed successfully; anonymous reminder calls return 401 and invalid plan bodies return 400.
- Local Vertex function calling was exercised with a combined crowd, walking-limit and route-choice request. Preference arguments and route IDs are validated on the server, and changes remain proposals until the user confirms them.
- Chat route-list requests use `display_routes`; the server accepts only supplied, unblocked route IDs and the client renders timing, arrival, legs, walking, transfers and crowd information from its computed plan.
- Companion data consent now appears as a first-open overlay instead of permanently occupying space above the message composer. Acceptance persists only on the device, can be revoked under **Preferences → Companion data sharing**, and returns on the next companion open after revocation or saved-data deletion.
- Companion requests opened before a fresh guest chooses a destination now omit the unfinished placeholder route instead of failing server validation. Rejected companion calls show the service error and only claim that route steps remain available when a computed plan exists; focused 320px and normal-phone browser regressions cover both paths.
- Companion chat now treats a planner result with no usable bundled-map route as empty optional context. The local or cloud companion can continue with general journey planning and preference help without route tools, while unrelated planner failures still surface as service errors and route-specific claims remain unavailable until a mapped plan exists.
- Companion voice input uses the browser Speech Recognition API after a microphone-button gesture, supports the standard and WebKit-prefixed implementations, and keeps the transcript as an editable draft until the user explicitly sends it. The interface discloses that the browser may use its vendor's online recognition service; microphone audio is not sent to Wayce's API.
- Physical-phone, spoken-audio listening, lock-screen push delivery and walked-route verification remain human QA tasks. Accessibility route certification, trained disruption probabilities and final submission materials are not claimed.
- Google’s real sign-in popup was exercised locally with an authorised Web OAuth client and a consenting test account. Preference and saved-commute writes reached Firestore, and logout followed by re-login restored the account state while keeping the guest space separate. Cross-device and deployed-production account sync remain unverified.

## Deployment status — 2026-09-19

- The user chose manual-only deployment from the authenticated development device. Both GitHub Actions workflows were removed; pushes and merges no longer start remote checks or deploy production. The local pre-push verification hook and automatic deployment verification were removed; deployment requires a new explicit request in chat.
- Tests remain available as optional manual checks, but direct-to-`main` pushes and the deployment script do not run them automatically.
- Cloud Build now uses the minimal `npm run build:deploy` container path: Vite client and esbuild server bundling only. Whole-project TypeScript, unit, browser and evaluation checks remain explicit local commands and no longer lengthen hosting deployments.
- `scripts/deploy-code.ps1` is the guarded code-update path: clean `main` source by default, protected-file upload inspection, Cloud Run source deployment, ready-revision inspection and application health check.
- The deployment path retains branch/tree guards, protected-upload inspection, attached service identities and post-rollout health checks. It labels the deployed service with its source commit and does not run local tests automatically.
- The Playwright launcher allocates a free local server port and a unique artifact directory for each run, passing them to every process through `WAYCE_E2E_PORT` and `WAYCE_E2E_RUN_ID`; both browser projects run in one invocation. This prevents concurrent or recently stopped runs from sharing fixed port 8081 or deleting one another's traces, and removes the second avoidable server restart.
- The device-local `$weliketrains-deploy` Codex skill records the same explicit-request workflow, failure diagnosis and post-deployment checks. It delegates routine mutation to the repository script and is optional personal tooling rather than a clean-clone dependency.
- The container build skips lifecycle scripts during dependency installation in the intentional Git-free build context. Temporary `gha-creds-*.json` files and the unrelated root `temp.txt` are excluded from Cloud Build and Docker contexts.
- The former GitHub Workload Identity resources may remain unused in the temporary GCP project. They were not torn down as part of this code change.
- The manual path was exercised after an explicit user request using the dirty-tree override. Regional Cloud Build `12683414-b6bb-41ce-a1af-54ea2f6f8472` succeeded, Cloud Run revision `weliketrains-00011-qzt` received 100% of traffic, `/api/health` returned `ok`, and the canonical public URL returned HTTP 200 with the Wayce page title. This proves the manual deployment path but not repository reproducibility until the deployed local changes are committed.
- After an explicit accelerated-deployment request, clean `main` commit `383a1cf` was deployed with a one-time browser-batch exception: the production build/typecheck and 33 unit tests passed, and the previously failing WCAG and disruption-alert browser checks passed individually, while the combined mobile run lost its local test server after its first passing flow and the remaining checks failed with `ERR_CONNECTION_REFUSED`. Upload inspection found no protected files. Regional Cloud Build `f2e44ac6-782f-46ae-8250-94154f31892c` succeeded, Cloud Run revision `weliketrains-00012-d8h` received 100% of traffic, `/api/health` returned `ok`, and the canonical public URL returned HTTP 200 with the Wayce page title. No dirty-tree or non-main override was used; the batch-runner failure remains a local test-infrastructure issue to repair.
- After the explicit map-performance deployment request, clean `main` commit `9d180b4` passed the production build, 33 committed unit tests and all 28 committed phone/wide browser checks. Healthy OneMap display no longer fetches or mounts the 4.39 MB OSM fallback; the fallback uses one Canvas when offline. A deployed Pixel-sized Chromium probe with 4× CPU throttling measured 262 DOM elements, no fallback request, no long tasks and a 16.7 ms p95 during the primary pan sequence; this remains synthetic browser evidence, not a physical-phone field test. Protected-upload inspection passed without overrides. Regional Cloud Build `bc39d92f-3cad-452c-8c27-7ceb65eebbdc` succeeded, Cloud Run revision `weliketrains-00013-xvh` became the latest ready revision with 100% traffic, `/api/health` returned `ok`, and the canonical root returned HTTP 200 with the expected Wayce title.

## Next session

Read `AGENTS.md`. Human polishing is pending. Do not restart the app architecture, introduce a desktop dashboard or prepare the deferred write-up. Do not recreate GitHub Actions or deploy without an explicit user request; use the guarded local deployment script when asked. Google model access can change: configured Gemini 3.5 returned 429, while Gemini 2.5 Flash worked in this project; the model remains environment-configurable.

Normal use now starts as Guest. Local Google account sync is configured and its sign-in/logout persistence flow has been exercised; production still needs the account environment and secret bindings deployed before claiming hosted account support. Developer-mode persona state must remain synthetic, visibly labelled and isolated from guest/account persistence.
